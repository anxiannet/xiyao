# 后端开发执行指令

只负责后端接口、规则引擎执行层、对局状态、日志与数据库接入。

读取：当前状态.md、规则总表.md、数据库README、程序代码/docs/README.md。

目标：支持玩家 vs AI、AI vs AI。

模块：api、engine、match、ai、logs、db。

配置表读取：xiyao_cfg_*。

运行表统一：xiyao_match_*。

接口：创建对局、读取对局、提交动作、AI行动、自动运行、读取日志。

动作类型仅允许：move、attack、skill、capture、end_turn。

禁止绕过规则引擎写结果。

禁止把规则塞进数据库字段。

输出：后端目录结构、运行表设计、API清单、事件流、AI接入流程、测试清单。